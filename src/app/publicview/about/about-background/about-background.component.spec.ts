/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AboutBackgroundComponent } from './about-background.component';

describe('Component: AboutBackground', () => {
  it('should create an instance', () => {
    let component = new AboutBackgroundComponent();
    expect(component).toBeTruthy();
  });
});
