/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ClientNotesItemComponent } from './client-notes-item.component';

describe('Component: ClientNotesItem', () => {
  it('should create an instance', () => {
    let component = new ClientNotesItemComponent();
    expect(component).toBeTruthy();
  });
});
